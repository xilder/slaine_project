{events.map((block, i) => (
          // <div key={block.year} className='mb-16 relative'>
          //   {/* Year marker */}
          //   <div className='flex justify-center mb-8'>
          //     <div className='relative flex items-center'>
          //       <div className='w-6 h-6 rounded-full bg-[#0044B5] border-4 border-white shadow-md z-10' />
          //       <span className='ml-4 text-xl font-bold text-gray-800'>
          //         {block.year}
          //       </span>
          //     </div>
          //   </div>

          //   {/* Events */}
          //   <div className='flex flex-col gap-12'>
          //     {block.items.map((item, idx) => {
          //       const isLeft = (idx + i) % 2 === 0;
          //       return (
          //         <motion.div
          //           key={idx}
          //           initial={{ opacity: 0, y: 50 }}
          //           whileInView={{ opacity: 1, y: 0 }}
          //           viewport={{ once: true, amount: 0.4 }}
          //           transition={{ duration: 0.6 }}
          //           className={`flex items-center ${
          //             isLeft ? 'justify-start' : 'justify-end'
          //           }`}
          //         >
          //           <div
          //             className={`max-w-sm p-4 rounded-2xl shadow-md bg-white ${
          //               isLeft ? 'mr-auto' : 'ml-auto'
          //             }`}
          //           >
          //             {item.type === 'text' ? (
          //               <p className='text-gray-700'>{item.content}</p>
          //             ) : (
          //               <Image
          //                 src={item.content}
          //                 alt={item.content}
          //                 width={400}
          //                 height={250}
          //                 className='rounded-lg'
          //               />
          //             )}
          //           </div>
          //         </motion.div>
          //       );
          //     })}
          //   </div>
          // </div>
        ))}